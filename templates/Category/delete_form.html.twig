{% include 'default/form.html.twig' with {
    'action': path('category_delete', {'id': category.id}),
    'form_id': 'delete-form-' ~ category.id,
    'csrf_token': csrf_token('delete-category' ~ category.id),
    'button_text': 'category.delete'|trans,
    'method': 'DELETE'
} %}
