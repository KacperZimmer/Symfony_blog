{# templates/post/delete_comment_form.html.twig #}
{% include 'default/form.html.twig' with {
    'action': path('comment_delete', {'id': comment.id}),
    'form_id': 'delete-form-' ~ comment.id,
    'csrf_token': csrf_token('delete-comment' ~ comment.id),
    'button_text': 'button.delete'|trans,
    'method': 'DELETE'
} %}
