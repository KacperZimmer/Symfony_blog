{% include 'default/form.html.twig' with {
    'action': path('post_delete', {'id': post.id}),
    'form_id': 'delete-form-' ~ post.id,
    'csrf_token': csrf_token('delete-post' ~ post.id),
    'button_text': 'button.delete'|trans,
    'method': 'DELETE'
} %}
