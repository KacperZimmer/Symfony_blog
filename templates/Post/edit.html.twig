{% extends 'base.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Edit Post</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary" type="submit">{{ form.vars.label }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
